import React from "react";
import job from "../images/job.png";

function Network() {
  return (
    <img
      src={job}
      alt="message"
      style={{
        maxWidth: "100%",
        height: "auto",
      }}
    />
  );
}

export default Network;
